# 16:9 Dashboard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rework the dashboard into a desktop 16:9 mission board.

**Architecture:** Keep the existing Next.js App Router and seed data. Replace only the dashboard route layout with a viewport-filling CSS grid and tighten shared chrome dimensions where needed.

**Tech Stack:** Next.js 16, React 19, TypeScript, Tailwind CSS v4, Node script layout verification.

## Global Constraints

- Use the existing design tokens from `src/app/globals.css`.
- Do not introduce new UI libraries.
- Keep `http://172.30.1.45:3000` as the user-facing local URL.

---

### Task 1: Layout Verification

**Files:**
- Create: `scripts/check-dashboard-layout.mjs`
- Modify: `package.json`

**Interfaces:**
- Produces: `npm run test:layout`, which exits non-zero when the dashboard is not a 16:9 board.

- [x] **Step 1: Write the failing test**

Create a Node script that reads `src/app/page.tsx` and asserts for a `dashboard-169-board` marker, fixed-height board classes, and removal of the old centered `max-w-[1280px]` wrapper.

- [x] **Step 2: Run test to verify it fails**

Run: `npm run test:layout`

Expected: FAIL before implementation because the marker does not exist.

- [ ] **Step 3: Implement the board layout**

Rewrite `src/app/page.tsx` into a compact grid with the `dashboard-169-board` marker.

- [ ] **Step 4: Run verification**

Run: `npm run test:layout` and `npm run build`.

- [ ] **Step 5: Inspect in browser**

Open `http://172.30.1.45:3000` and verify the first screen is filled by the board without obvious overlap.
